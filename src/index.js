import ReactDOM from 'react-dom'; // This is needed to use ReactDOM.render();
import React from 'react'; // This is needed if you want to use JSX syntax or extends React.Component

// TODO: Render the `App` component to the DOM
import App from './components/App.js';

ReactDOM.render(<App />, document.GetElementByID('app'));